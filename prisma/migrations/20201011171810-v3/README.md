# Migration `20201011171810-v3`

This migration has been generated by Harsh Rumalwala at 10/11/2020, 10:48:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "_RoomLanguageMapping" (
    "A" INTEGER NOT NULL,
    "B" TEXT NOT NULL,

    FOREIGN KEY ("A") REFERENCES "Language"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("B") REFERENCES "Room"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Room" (
    "id" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "round" INTEGER NOT NULL,
    "roundLimit" INTEGER NOT NULL,
    "roundStartedAt" DATETIME,
    "roundMovieId" INTEGER,
PRIMARY KEY ("id")
);
INSERT INTO "new_Room" ("id", "createdAt", "round", "roundLimit", "roundMovieId", "roundStartedAt") SELECT "id", "createdAt", "round", "roundLimit", "roundMovieId", "roundStartedAt" FROM "Room";
DROP TABLE "Room";
ALTER TABLE "new_Room" RENAME TO "Room";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

CREATE UNIQUE INDEX "_RoomLanguageMapping_AB_unique" ON "_RoomLanguageMapping"("A", "B")

CREATE INDEX "_RoomLanguageMapping_B_index" ON "_RoomLanguageMapping"("B")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201011161916-v2..20201011171810-v3
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider        = "prisma-client-js"
@@ -39,9 +39,9 @@
 model Language {
   id     Int     @id @default(autoincrement())
   name   String  @unique
   movies Movie[] @relation("LanguageMapping")
-  rooms  Room[]
+  rooms  Room[]  @relation("RoomLanguageMapping")
 }
 model User {
   id             Int      @id @default(autoincrement())
@@ -56,17 +56,16 @@
   roomId         String?
 }
 model Room {
-  id             String    @id @default(uuid())
-  createdAt      DateTime  @default(now())
-  players        User[]    @relation("PlayerMapping")
-  host           User?     @relation("HostMapping")
+  id             String     @id @default(uuid())
+  createdAt      DateTime   @default(now())
+  players        User[]     @relation("PlayerMapping")
+  host           User?      @relation("HostMapping")
   round          Int
   roundLimit     Int
   roundStartedAt DateTime?
   roundCompleted User[]
   roundMovieId   Int?
-  language       Language? @relation(fields: [languageId], references: [id])
-  languageId     Int?
-  movies         Movie[]   @relation("RoomMapping")
+  languages       Language[] @relation("RoomLanguageMapping")
+  movies         Movie[]    @relation("RoomMapping")
 }
```


