# Migration `20201002054408-v2`

This migration has been generated by Harsh Rumalwala at 10/2/2020, 11:14:08 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Room" (
    "id" TEXT NOT NULL,
    "round" INTEGER NOT NULL,
    "roundLimit" INTEGER NOT NULL,
    "movieId" INTEGER NOT NULL,
    "languageId" INTEGER NOT NULL,

    FOREIGN KEY ("languageId") REFERENCES "Language"("id") ON DELETE CASCADE ON UPDATE CASCADE,
PRIMARY KEY ("id")
);
INSERT INTO "new_Room" ("id", "round", "roundLimit", "movieId") SELECT "id", "round", "roundLimit", "movieId" FROM "Room";
DROP TABLE "Room";
ALTER TABLE "new_Room" RENAME TO "Room";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200927125750-v1..20201002054408-v2
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider        = "prisma-client-js"
@@ -38,8 +38,9 @@
 model Language {
   id     Int     @id @default(autoincrement())
   name   String  @unique
   movies Movie[] @relation("LanguageMapping")
+  rooms  Room[]
 }
 model User {
   id             Int     @id @default(autoincrement())
@@ -53,12 +54,14 @@
   roomId         String?
 }
 model Room {
-  id             String @id @default(uuid())
-  players        User[] @relation("PlayerMapping")
-  host           User   @relation("HostMapping")
+  id             String   @id @default(uuid())
+  players        User[]   @relation("PlayerMapping")
+  host           User     @relation("HostMapping")
   round          Int
   roundLimit     Int
   roundCompleted User[]
   movieId        Int
+  language       Language @relation(fields: [languageId], references: [id])
+  languageId     Int
 }
```


