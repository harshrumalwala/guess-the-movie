# Migration `20201002140919-v4`

This migration has been generated by Harsh Rumalwala at 10/2/2020, 7:39:19 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "_RoomMapping" (
    "A" INTEGER NOT NULL,
    "B" TEXT NOT NULL,

    FOREIGN KEY ("A") REFERENCES "Movie"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("B") REFERENCES "Room"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Room" (
    "id" TEXT NOT NULL,
    "round" INTEGER NOT NULL,
    "roundLimit" INTEGER NOT NULL,
    "roundMovieId" INTEGER,
    "languageId" INTEGER,

    FOREIGN KEY ("languageId") REFERENCES "Language"("id") ON DELETE SET NULL ON UPDATE CASCADE,
PRIMARY KEY ("id")
);
INSERT INTO "new_Room" ("id", "round", "roundLimit", "languageId") SELECT "id", "round", "roundLimit", "languageId" FROM "Room";
DROP TABLE "Room";
ALTER TABLE "new_Room" RENAME TO "Room";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

CREATE UNIQUE INDEX "_RoomMapping_AB_unique" ON "_RoomMapping"("A", "B")

CREATE INDEX "_RoomMapping_B_index" ON "_RoomMapping"("B")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201002074052-v3..20201002140919-v4
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider        = "prisma-client-js"
@@ -19,8 +19,9 @@
   genre       Genre[]  @relation("GenreMapping", references: [id])
   boxOffice   Float
   language    Language @relation("LanguageMapping", fields: [languageId], references: [id])
   languageId  Int
+  rooms       Room[]   @relation("RoomMapping", references: [id])
 }
 model Person {
   id       Int     @id @default(autoincrement())
@@ -60,8 +61,9 @@
   host           User      @relation("HostMapping")
   round          Int
   roundLimit     Int
   roundCompleted User[]
-  movieId        Int?
+  roundMovieId   Int?
   language       Language? @relation(fields: [languageId], references: [id])
   languageId     Int?
+  movies         Movie[]   @relation("RoomMapping")
 }
```


