# Migration `20201002074052-v3`

This migration has been generated by Harsh Rumalwala at 10/2/2020, 1:10:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Room" (
    "id" TEXT NOT NULL,
    "round" INTEGER NOT NULL,
    "roundLimit" INTEGER NOT NULL,
    "movieId" INTEGER,
    "languageId" INTEGER,

    FOREIGN KEY ("languageId") REFERENCES "Language"("id") ON DELETE SET NULL ON UPDATE CASCADE,
PRIMARY KEY ("id")
);
INSERT INTO "new_Room" ("id", "round", "roundLimit", "movieId", "languageId") SELECT "id", "round", "roundLimit", "movieId", "languageId" FROM "Room";
DROP TABLE "Room";
ALTER TABLE "new_Room" RENAME TO "Room";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201002054408-v2..20201002074052-v3
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider        = "prisma-client-js"
@@ -54,14 +54,14 @@
   roomId         String?
 }
 model Room {
-  id             String   @id @default(uuid())
-  players        User[]   @relation("PlayerMapping")
-  host           User     @relation("HostMapping")
+  id             String    @id @default(uuid())
+  players        User[]    @relation("PlayerMapping")
+  host           User      @relation("HostMapping")
   round          Int
   roundLimit     Int
   roundCompleted User[]
-  movieId        Int
-  language       Language @relation(fields: [languageId], references: [id])
-  languageId     Int
+  movieId        Int?
+  language       Language? @relation(fields: [languageId], references: [id])
+  languageId     Int?
 }
```


