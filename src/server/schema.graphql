scalar Date

type Query {
  movies: [Movie!]!
  persons: [Person!]!
  movie(id: Int!): Movie!
  person(id: Int!): Person!
}

type Mutation {
  add(
    name: String!
    director: PersonInput!
    genre: String!
    cast: [PersonInput!]!
    releaseDate: Date!
  ): Movie!
  update(
    id: Int!
    name: String
    director: PersonInput
    genre: String
    cast: [PersonInput!]
    releaseDate: Date
  ): Movie
  remove(id: Int!): Movie
}

type Movie {
  id: Int!
  name: String!
  director: Person!
  genre: String!
  cast: [Person!]!
  releaseDate: Date!
}

type Person {
  id: Int!
  name: String!
  acted: [Movie!]
  directed: [Movie!]
}

input MovieInput {
  id: Int
  name: String
  director: PersonInput
  genre: String
  cast: [PersonInput]
  releaseDate: Date
}

input PersonInput {
  id: Int
  name: String
  acted: [MovieInput]
  directed: [MovieInput]
}
